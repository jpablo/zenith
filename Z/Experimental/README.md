# Reserved experimental folder

This folder is currently empty. It is reserved for experiments that may later
become core `Z` features.

Experimental code must not be imported by `Z.lean` until its API and runtime
semantics are stable.
